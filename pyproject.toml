[project]
name = "rtmdet-rotated"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beartype>=0.22.9",
    "jaxtyping>=0.3.9",
    "numpy>=2.4.3",
    "torch>=2.10.0",
    "torchvision>=0.25.0",
]

[project.optional-dependencies]
test = [
    "pytest>=9.0.2",
]

[dependency-groups]
dev = [
    "basedpyright>=1.38.3",
    "fire>=0.7.1",
]
test = [
    "pytest>=9.0.2",
]
